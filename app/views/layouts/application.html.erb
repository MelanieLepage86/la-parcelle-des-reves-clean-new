<!DOCTYPE html>
<html>
  <head>
    <title>Laparcelledesreves</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <script src="https://js.stripe.com/v3/"></script>
    <%= javascript_importmap_tags %>
    <link href="https://fonts.cdnfonts.com/css/gistesy" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Borel&family=Cormorant+Garamond:ital,wght@0,300..700;1,300..700&family=Covered+By+Your+Grace&family=Intel+One+Mono:ital,wght@0,300..700;1,300..700&family=Kings&display=swap" rel="stylesheet">
  </head>

  <body class="d-flex flex-column min-vh-100">
    <% unless current_page?(root_path) %>
      <%= render "shared/navbar" %>
    <% end %>

    <main class="flex-fill">
      <%= render "shared/flashes" %>
      <%= yield %>
    </main>

    <% unless current_page?(root_path) %>
      <%= render "shared/footer" %>
    <% end %>
  </body>
</html>
